// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
  url = env("DATABASE_URL")
}

model GameTable {
   
}

// idea is to create tables like 
user table 
-- serial no
-- username 
-- userid
-- profile picture

game table
-- serial no
-- gameid
-- whiteplayerId
-- blackPlayerId
-- match status
-- who won (No_one, white, black)

moves table
-- serial no
-- gameid
-- from and to
-- who made that move (playerId)

(Only store valid moves)
